{
  "name": "h2jk",
  "version": "0.0.1",
  "description": "HubSpot to Jekyll",
  "main": "/src/index.js",
  "scripts": {
    "build": "npm run clean && npm run pretty && babel src --out-dir lib",
    "coverage": "nyc report --reporter=text-lcov | coveralls",
    "clean": "rimraf coverage lib test-dir",
    "dev": "npm run test:watch",
    "test:coverage": "nyc --reporter=html --reporter=text -- npm run test -- --reporter dot",
    "pretty": "prettier '{src,test}/**/*.js' --single-quote --trailing-comma es5 --write",
    "prepare": "npm run test && npm run build",
    "start": "npm run dev",
    "test": "npm run clean && npm run test:specs",
    "test:specs": "mocha test/setup.mocha.js --recursive src/**/*.spec.js",
    "test:watch": "npm run pretty && npm run test -- --watch --reporter dot"
  },
  "dependencies": {
    "lodash": "4.17.4",
    "mkdirp": "0.5.1"
  },
  "devDependencies": {
    "babel-cli": "6.24.1",
    "babel-core": "6.24.1",
    "babel-preset-es2015": "6.24.1",
    "chai": "4.0.2",
    "coveralls": "2.13.1",
    "mocha": "3.4.2",
    "mocha-lcov-reporter": "1.3.0",
    "nyc": "10.3.2",
    "prettier": "1.5.2",
    "rimraf": "2.6.1"
  },
  "author": "ItsJonQ <itsjonq@gmail.com> (http://jonquach.com)",
  "license": "MIT"
}
